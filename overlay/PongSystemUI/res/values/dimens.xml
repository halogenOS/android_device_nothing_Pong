<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2024 Paranoid Android
     SPDX-License-Identifier: Apache-2.0
-->
<resources>

    <!-- The padding on the top of the statusbar (usually 0) -->
    <dimen name="status_bar_padding_top">8.4dp</dimen>

    <!-- The padding on the start of the statusbar -->
    <dimen name="status_bar_padding_start">12.2dp</dimen>

    <!-- The padding on the end of the statusbar -->
    <dimen name="status_bar_padding_end">6.1dp</dimen>

    <!-- Height of the status bar header bar when on Keyguard (match status_bar_height) -->
    <dimen name="status_bar_header_height_keyguard">@*android:dimen/status_bar_height</dimen>

    <!-- Margin on the left side of the carrier text on Keyguard -->
    <dimen name="keyguard_carrier_text_margin">15.2dp</dimen>

    <!-- Margin on the bottom of the indication text on Keyguard -->
    <dimen name="keyguard_indication_margin_bottom">121.9dp</dimen>

    <!-- Location on the screen of the center of the physical power button. -->
    <dimen name="physical_power_button_center_screen_location_y">384.4dp</dimen>

    <!-- Microns/ums (1000 um = 1mm) per pixel for the given device. If unspecified, UI that
        relies on this value will not be sized correctly. -->
    <item name="pixel_pitch" format="float" type="dimen">64.4</item>

    <!-- For burn-in protection -->
    <dimen name="status_bar_offset_max_y">2dp</dimen>
    <dimen name="burn_in_prevention_offset_x">2dp</dimen>
    <dimen name="burn_in_prevention_offset_y">4dp</dimen>
    <dimen name="burn_in_prevention_offset_y_clock">16dp</dimen>
    <dimen name="default_burn_in_prevention_offset">4dp</dimen>
    <dimen name="udfps_burn_in_offset_x">6px</dimen>
    <dimen name="udfps_burn_in_offset_y">8px</dimen>

</resources>
